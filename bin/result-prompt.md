# Result Review Prompt

Below is the implementation report (and any follow-ups so far) for the
plan we worked out in this conversation. Compare it against that plan
and its acceptance criteria.

List any problems or gaps you find. If follow-up work is needed, output
ONLY the follow-up instructions, ready to copy, in this format (body
only — no heading, the tooling adds the `## Follow-up N` heading):

- Optional 1-2 line goal.
- Then numbered items: `N. <file> — <precise change>`.
- Terse, sentence fragments are fine, no restating context already given.

If nothing needs fixing, say so explicitly instead of inventing work.

The human may also paste their own test findings below the
implementation report — take those into account too.

For mobile work, treat skipped platform/flavor builds, device smoke tests,
permission/lifecycle checks, accessibility/localization checks, and
upgrade testing as explicit gaps when they are relevant to the changed behavior.

Some sections below may be provided as attached files instead of pasted
inline — read them too.

Below is the implementation report:
